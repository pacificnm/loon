[package]
name = "loon-player"
version = "0.1.0"
edition = "2021"
description = "External video player launcher for Loon desktop (ffplay / mpv)"

[dependencies]
nest-error = { workspace = true }
tracing = { workspace = true }
urlencoding = "2"

[dev-dependencies]
tempfile = "3"
