# Loon Admin Desktop v1 — Plan

## Status: Complete (Tauri + React)

Desktop admin GUI for library management, TMDB matching, scans, and server settings. Complements the webOS client admin screen with a keyboard/mouse-friendly desktop experience.

## Goals

- Manage Loon server from a native desktop window (scan, match, library paths, logs)
- Built with Tauri 2 + React + Tailwind CSS, using Nest `nest-tauri` and `nest-theme`
- Loon-branded dark theme aligned with webOS client (`#090B10` / `#7DD3FC`)
- Talk to existing Loon HTTP API (`loon-server`) — no duplicate business logic

## Non-goals (v1)

- Replace webOS client
- Embed server in the GUI process (client/server stay separate)
- Full design system / component library (`nest-ui` deferred)
- Docking, command palette, in-app log viewer

## Crate boundaries

| Crate | Depends on | Owns |
|-------|------------|------|
| `loon-admin-tauri` (`src-tauri/`) | `nest-tauri`, `nest-theme`, `nest-image`, `nest-cache-file` | Tauri IPC, image caching, theme |
| `loon-admin-ui` (`ui/`) | React, Tailwind, `@tauri-apps/api` | UI components, navigation, HTTP client to server |
| `loon-server` | (existing) | API, scan, SQLite, streaming |

## Architecture

```text
┌─────────────────────────────────────────────────────────┐
│ Loon Admin                                    [— □ ×]   │
├──────────┬──────────────────────────────────────────────┤
│ Library  │  Main content (movies / detail / scan)       │
│ Scan     │                                              │
│ Settings │                                              │
└──────────┴──────────────────────────────────────────────┘
```

```text
┌─────────────────────┐
│  React Frontend     │
│  (ui/)              │
│  - AppShell         │
│  - LibraryPanel     │
│  - MovieDetail      │
│  - ScanPanel        │
│  - SettingsPanel    │
└─────────┬───────────┘
          │ HTTP / Tauri IPC
          │
┌─────────▼───────────┐
│  Tauri Backend      │
│  (src-tauri/)       │
│  - Image caching    │
│  - Theme module     │
└─────────┬───────────┘
          │ HTTP
          │
┌─────────▼───────────┐
│  Loon Server        │
│  (API)              │
└─────────────────────┘
```

## Implementation status

### Complete

- [x] Tauri 2 + React workspace (`src-tauri/` + `ui/`)
- [x] Loon dark theme (`#090B10` / `#7DD3FC`) via `nest-theme`
- [x] App shell with sidebar navigation
- [x] Server connection via `useApi` hook
- [x] Library panel with movie grid
- [x] Movie detail view
- [x] Scan panel for triggering library scans
- [x] Settings panel
- [x] Image caching via `nest-cache-file`

### In progress / Future

- [ ] Health check + connection status indicator
- [ ] Library status panel (movie count, last scan time)
- [ ] Manual TMDB rematch (`PUT /api/movies/:slug/match`)
- [ ] Locked-match indicator
- [ ] Error toasts for API failures
- [ ] Packaging (deb/rpm/appimage on Linux)

## Configuration

```toml
[gui]
title = "Loon Admin"
width = 1280
height = 800

[loon-admin]
server_url = "http://127.0.0.1:3000"
mpv_path = "mpv"
```

## Quick start

```bash
cd apps/loon/desktop

# Install dependencies
npm install --prefix ui

# Run in development mode
npm run dev --prefix ui

# Or build Tauri app
npm run tauri dev
```

## Open questions

- Auth for admin API (local network trust vs. token later)
- Whether to extract shared API types crate from webOS client
